/********逢七过********/
//循环100次
for (var i = 1; i <= 100; i++) {
    //判断是否为7的倍数 且 不包含"7"
    if (i % 7 === 0 || i.toString().indexOf("7") != -1){
        //如果条件满足则执行
        console.log("过");
    }else{
        //如果条件不满足则执行
        console.log(i);
    }
}