/********九九乘法表********/
//新建变量 储存乘法表
var a = "";
//循环体 行 循环九次
for (var i = 1; i <= 9; i++) {
    //循环体 列 次数累加
    for (var j = 1; j <= i; j++) {
        //计算 并拼接
        a += (i + "*" + j + "=" + (i*j) + " ");
    }
    // 打印换行符
    a += "\n";
}
//打印结果
console.log(a);